{# <!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome Alison!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block body %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Rechercher</button>
        {{ form_end(form) }}

        {% if terrains is not empty %}
        <h2>Terrains correspondants :</h2>
        <ul>
            {% for terrain in terrains %}
            <li>
                <strong>Nom :</strong> {{ terrain.getNom()}}<br>
                <strong>Adresse :</strong> {{ terrain.getAdresse()}}<br>
                <strong>Ville :</strong> {{ terrain.getVille()}}
            </li>
            {% endfor %}
        </ul>
        {% endif %}
        {% endblock %}
    </body>
</html> #}

<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome !{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        {% block body %}
        <form method="post">
            {{ form_widget(form) }}
            <button type="submit">Rechercher</button>
        </form>

        {% if terrains is not empty %}
        <h2>Terrains correspondants :</h2>
        <ul>
            {% for terrain in terrains %}
            <li>
                <strong>Nom :</strong> {{ terrain.getNom() }}<br>
                <strong>Adresse :</strong> {{ terrain.getAdresse() }}<br>
                <strong>Ville :</strong> {{ terrain.getVille() }}
            </li>
            {% endfor %}
        </ul>
        {% endif %}
        {% endblock %}
    </body>
</html>
